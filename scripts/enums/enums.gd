class_name Enums

enum Realm {
	DIVINE,
	VOID,
	ELEMENTAL,
	NATURE,
	MORTAL
}

enum ResourceType {
	GOLD,
	IRON,
	TECHNOLOGY,
	FOOD,
	SHARD_ESSENCE,
	WOOD,
	CAPTIVES
}

enum BattleOrder {
	ADVANCE,
	HOLD,
	FLANK_LEFT,
	FLANK_RIGHT,
	CHARGE,
	RETREAT
}

enum TerrainType {
	PLAINS,
	FOREST,
	MOUNTAINS,
	DESERT,
	SWAMP,
	COAST,
	TUNDRA,
	SHARD_WASTES,
	WATER,
	JUNGLE
}

enum UnitStance {
	AGGRESSIVE,
	DEFENSIVE,
	FLANKING,
	SUPPORT
}

enum TargetPriority {
	CLOSEST,
	WEAKEST,
	STRONGEST,
	RANGED_FIRST,
	SUPPORT_FIRST
}

enum FactionRelation {
	WAR,
	HOSTILE,
	NEUTRAL,
	FRIENDLY,
	ALLIED
}

enum GamePhase {
	MAIN_MENU,
	CAMPAIGN,
	BATTLE_SETUP,
	BATTLE_SIMULATION,
	BATTLE_RESULT,
	EVENT
}

enum BattleTerrain {
	OPEN,
	FOREST,
	ROCK,
	WATER,
	SAND,
	MUD,
	ICE,
	CRYSTAL,
	BRUSH
}

enum FormationShape { LINE, LOOSE_LINE, WEDGE, BLOCK, SINGLE }

enum DiplomacyAction { DECLARE_WAR, PROPOSE_PEACE, PROPOSE_ALLIANCE, OFFER_TRADE, GIFT_RESOURCES, OFFER_SHARD }
enum TreatyType { PEACE, ALLIANCE, TRADE_DEAL }
enum PolicyCategory { TAXATION, MILITARY, CULTURAL, LABOR }
enum VictoryType { NONE, DOMINATION, DIPLOMATIC, SHARD_ASCENSION, ELIMINATION, DEFEAT }
